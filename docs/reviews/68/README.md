# Issue #68 デザイン修正の確認記録

2026-09-08。PR #66 の実装を引き継ぎ、PSD と実画面の目視比較を基準に修正。

## 確認基準

無料フォントへの置換による字形差は許容し、配置・文字サイズ・配色・余白・装飾をカンプに合わせる。
一致率の数値だけでは合格としない。固定ヘッダーを含むPC/SP通し画像、開閉状態、中間幅を確認する。

## 修正

- セクションラベルの文字寸法・斜体・細線。背景に含まれる線の二重描画を除去。
- 見出しのグラデーションと黄／白の強調、本文のサイズと折り返し。
- SKILLSの固定幅タグ、カード配置、SP開閉に伴う後続カードの移動。
- DIFFERENCEの表の列・行・罫線、当社見出しの段差、POINT番号。
- RESULTSの両端画像欠落と、説明文によるSP矢印クリックの遮断。
- PRICING・VOICE・PRESENTの文字位置、FAQとVOICEの展開時の高さ。
- PCのFV回路帯とCTA画像。ボタンの読ませる文字はHTMLを維持。
- 設計幅375/1440からの等比拡縮。1440px超の中央配置、未対応ブラウザ向け補完。
- Jicooの実フォームを不透明なカードに配置し、外部iframeの高さ変化に追従。
- 撮影時にフォントを待ち、撮影画像と同時点のDOM座標を同じディレクトリへ保存。

## 検証

- `npm run lint`、`npm run typecheck`、`npm run build` が成功。
- `LP2_URL=http://localhost:3100/siid/lp-2 node scripts/lp2/check-layout.mjs`：5項目成功。
  - SPカード開閉、後続カードの移動、本文がカード内に収まること。
  - SPの実績矢印を通常クリックできること。
  - PCの実績カルーセルに5枚描画されること。
  - SP/PCの予約フォームを800px高にしても下端が切れないこと。
- 320/375/390/768/1024/1280/1440/1920pxで横へのはみ出しなし。
- 幅変更後のPC/SP切替、Swiper操作、アンカー先と固定ヘッダー、2倍ピンチを確認。
- SKILLS全件展開、VOICE全件展開、FAQ展開をPC/SPで撮影。
- `/siid/lp-1`、`/siid/`、`/siid/service` はHTTP 200、ブラウザ実行エラーなし。
- 実装と別の担当がセクションを相互レビューし、クリック遮断とSP表の追加指摘を修正・再検証。

## 確認の制約

- この検証環境ではJicooの空き日程APIがCORSエラーとなった。iframeの挿入と高さ更新、
  ページレイアウトは確認できたが、実際の空き日程選択・予約送信は検証していない。
  通し画像では画面外iframeが白く撮影されることがあるため、表示の合否はフォーム位置へスクロールして確認する。
- 本番ビルドには変更対象外の404用Phaserの既存import警告が出る。
- ローカルにはmicroCMS環境変数を設定していないため、ニュースの外部データ取得は検証対象外。
- 既定のFV動画、無料フォント、仮キャプション、別IssueのCV計測、公開後の確認事項は引き継ぎの判断を維持。

## 最終画面

画像はGitHubで確認しやすいよう縮小している。PCは1440px、SPは375pxで撮影。
実際のフォーム内容と展開時のセクション高はカンプの固定見本とは異なる。

[PC通し画像](./pc.webp) / [SP通し画像](./sp.webp)

[SKILLSのカンプ比較](./skills.webp) / [DIFFERENCEのカンプ比較](./difference.webp)

## 2026-09-09 追加修正

- PC用CTA画像に含まれる角丸外側の白い背景を、共通CTAの表示範囲から除外。青い背景のSUPPORTと白い料金カードで見え方が違っていた原因を修正。
- 切り抜きは比率指定とし、小さい料金カードのCTAでも同じ形状を維持。SP本文の背景埋め込みボタン・ヘッダーボタンは対象外。
- INSTRUCTORのPC箇条書き3行を8px上へ調整し、背景内の四角アイコンと文字の中心を一致させた。
- DIFFERENCE比較表は透明なセル間隔を廃止し、通常セルを白罫線、当社列を黒罫線へ統一。PCでは2px、SPでは1pxとする。
- SKILLS見出し下の直線を、カンプの折り返し・かすれ・曲線をなぞった装飾SVGへ置換。PC/SPで同じ形状を拡縮する。
- SUPPORTの「その学び」「国が最大80%負担します」を紫から水色への連続グラデーションに修正。小さい文字の固定色も除去し、文字列全幅に色の変化を適用。
- SUPPORTの講座名「第四次産業革命スキル習得講座」にも、文字全幅の紫→水色グラデーションを適用。隣の「認定」はカンプどおり白を維持。
- PRICINGのCTA文字に継承されていた大きい字間を解除。文字サイズを維持し、左右の矢印との余白を3プラン共通で確保。
- PC料金カードのCTAを、pc5.psdの専用レイヤーから書き出した `plan-cta.png` へ変更。共通画像の単色行補間で失われていた横方向の濃淡と細かな質感を維持。文字はHTML。再生成は `python scripts/lp2/export-plan-cta.py`（PSD解析用環境と `tmp/psd-ref/pc/pc5.png` が必要）。

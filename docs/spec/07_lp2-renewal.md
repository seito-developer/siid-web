# 07. LP リニューアル(`/siid/lp-2`)実装仕様

新規デザイン(Photoshop 入稿)に基づく広告流入用 LP を `(Lp)` ルートグループ配下に新規追加する。
既存 LP `/siid/lp-1` および `(Main)` 配下の既存サイトには一切手を加えない。

作成日: 2026-09-06

---

## 1. 背景・目的

- 現行 LP `/siid/lp-1` は旧デザインを React 化したもの(Issue #40)。
- 新しく Photoshop でデザイン一式が入稿されたため、これを新 LP として実装する。
- 検証・切り戻しを可能にするため、旧 LP を残したまま `/siid/lp-2` として並走させる。

### 1.1 決定事項(ヒアリング済み)

| 項目 | 決定 |
|------|------|
| 配置 | 既存リポジトリ `siid-web` の `src/app/(Lp)/lp-2/`。`(Main)` は変更しない |
| URL | `/siid/lp-2`(`basePath: '/siid'` 配下) |
| 旧 LP | `/siid/lp-1` は残す。将来の削除は別 Issue |
| ページ数 | **1 ページのみ**。下層ページ・ルーティングは持たない |
| スタイル | CSS Modules(セクション単位)＋ トークン用グローバル CSS |
| 予約フォーム | 既存 LP と同じ **Jicoo** 埋め込みを流用 |
| フッター | **デザインに存在しないため新規設計して追加**(§10) |
| スクロールアニメーション | **なし**(デザイナー指定なし) |
| FV 背景 | 支給された動画を使う。**カンプ(Zoom 画面の仮置き)と異なってよい** |
| フォント | 商用フォントは使用不可のため無料 Web フォントへ置換(§6) |
| noindex | 未確定。実装は index 前提で進め、公開直前に切り替え可能にする(§11) |
| Cookie 同意バナー | **実装する**(GA4 / GTM を入れるため)。§13.4 |
| CTA のリンク先 | すべてページ内アンカー `#counselling` |
| VOICE の「続きを読む」全文 | **仮テキストを入れる**。公開前に実原稿へ差し替え(§15) |

---

## 2. 入稿データ

`/Users/horiguchimasato/Downloads/260902_seitosama/`

```
pc/   pc1.psd 〜 pc9.psd, pc_link.psb(1440x17405 = 全体結合)
sp/   seitosama_lp_sp01.psd 〜 sp11.psd, sp12_menu.psd, sp_link.psb
images/, webp_x2/   カルーセル画像の書き出し済みアセット(webp / @2x)
```

- **PC: 1440px 幅・等倍(1x)**。総高 17,404px。
- **SP: 750px 幅・@2x**(CSS 375px 相当)。総高 33,898px = CSS 16,949px。
- `pc/pc1-assets/errors.txt` に生成アセット書き出しの失敗ログあり(レイヤー名重複)。
  書き出し済みアセットは信用せず、**PSD から再書き出しする**(§8)。

### 2.1 重要な制約: PC 版が等倍である

PC 版 PSD は 1440px = 等倍で作られているため、**PSD からテキストやパーツをラスタ画像として切り出しても Retina 表示に必要な 2 倍解像度が得られない**。

主要見出しの検証結果(必要解像度に対する入手可能な最大素材):

| 文字列 | PC 表示サイズ | 入手可能な最大素材 | 判定 |
|--------|--------------|------------------|------|
| 選ばれるエンジニアへ | 79.4px | 79.4px | 不足 |
| 未経験から、ここまで目指せる！ | 58.2px | 58.2px | 不足 |
| よくある質問 / 受講生の声 | 60.1px | 60.1px | 不足 |
| つのプラン | 64.6px | 64.6px | 不足 |
| 無料カウンセリングを(CTA) | 32.2px | 42.9px(SP 版流用) | 不足 |
| GIFT01 | 16.0px | 32.0px(SP 版流用) | 充足 |

→ **見出しの画像化(アウトライン化)方式は採用できない。** テキストは全て実テキストで実装し、フォントを置換する(§6)。

---

## 3. セクション構成とデザインデータ対応表

PSD のレイヤーグループは `sec_*` でセマンティックに命名されており、そのままコンポーネント分割に使う。

| # | セクション | PC ファイル | SP ファイル | 主な内容 |
|---|-----------|------------|------------|---------|
| 1 | `fv` | pc1 | sp01 | 動画背景 + メインコピー + 実績バッジ(88% / 92% / 13万人) + CTA |
| 2 | `sec_about` | pc1 | sp01 | 「企業が求める AI 人材」へ |
| 3 | `sec_result` | pc1 | sp01 | RESULTS。3D 風カルーセル + ページネーション |
| 4 | `sec_instructor` | pc2 | sp01 | INSTRUCTOR。講師(堀口セイト)紹介 + CTA |
| 5 | `sec_strength` | pc2 | sp01 | STRENGTH。3 つの強み(カード 3 枚) |
| 6 | `sec_difference` / `sec_comparison` | pc2 | sp02 | DIFFERENCE。他社比較 POINT 3 枚 + 比較表(6 行 × 4 列) |
| 7 | `sec_step` | pc3 | sp03 | 内定までの 5STEP(縦フロー) + CTA |
| 8 | `sec_skill` | pc4 | sp04 | SKILLS。技術カード 5 枚 |
| 9 | `sec_support` | pc4 | sp05 | RESKILLING SUPPORT。給付金・経産省認定 + CTA |
| 10 | `sec_plan` | pc5 | sp06 | PRICING。料金 3 プラン(中央 = おすすめ) |
| 11 | `sec_graph` | pc5 | sp07 | コース比較表(12 行 × 3 列) |
| 12 | `sec_voice` | pc6 | sp08 | VOICE。受講生の声 3 件(「続きを読む」あり) |
| 13 | `sec_present` | pc7 | sp10 | FREE GIFTS。7 大特典 + CTA |
| 14 | FAQ(`ttl` + `Q1`〜`Q8`) | pc8 | sp09 | アコーディオン 8 問 |
| 15 | `sec_counselling` | pc9 | sp11 | FREE COUNSELING。Jicoo 予約フォーム |
| — | ヘッダー(固定ナビ) | pc1 | sp01 | ロゴ + 強み/実績/料金/特典/FAQ + CTA |
| — | SP ドロワー | — | sp12_menu | ハンバーガーメニュー |
| — | フッター | **なし** | **なし** | 新規設計(§10) |

### 3.1 PC と SP でセクション順序が食い違っている(決定済み)

- **PC**: … → VOICE(pc6) → FREE GIFTS(pc7) → FAQ(pc8) → COUNSELING(pc9)
- **SP**: … → VOICE(sp08) → FAQ(sp09) → FREE GIFTS(sp10) → COUNSELING(sp11)

→ **SP の順序を正とする**(ヒアリングで決定)。PC も以下の順序に揃えて実装する。

```
… → VOICE → FAQ → FREE GIFTS → FREE COUNSELING → フッター
```

§3 の一覧表の通し番号(13: FREE GIFTS / 14: FAQ)は PC ファイルの並び順に基づくものであり、
**実装時の DOM 順序は FAQ → FREE GIFTS** となる点に注意する。

---

## 4. 配置とファイル構成

既存の `(Lp)` ルートグループは `(Main)` と独立した root layout を持つ(Issue #40)。この隔離をそのまま利用する。

```
src/app/(Lp)/
├── layout.tsx          # 既存。lp-1 / lp-2 共通の root layout
├── fonts.ts            # 既存。lp-2 用フォントを追記する
├── lp1.css             # 既存(lp-1 専用)。変更しない
├── lp-1/page.tsx       # 既存。変更しない
└── lp-2/
    ├── page.tsx        # 新規。セクションコンポーネントを並べるだけ
    └── lp2-tokens.css  # 新規。デザイントークン(:root)のみ

src/components/Lp2/
├── Header/            Header.tsx        + Header.module.css
├── DrawerMenu/        …                 # SP ハンバーガー(sp12_menu)
├── Fv/
├── About/
├── Result/
├── Instructor/
├── Strength/
├── Difference/
├── Step/
├── Skill/
├── Support/
├── Plan/
├── Graph/
├── Voice/
├── Present/
├── Faq/
├── Counselling/       # Jicoo 埋め込み
├── Footer/            # 新規設計
├── CtaButton/         # 全セクションで再利用する CTA
└── SectionLabel/      # 英字ラベル + 両端の線飾り(RESULTS / VOICE 等)
```

### 4.1 layout の扱い

`(Lp)/layout.tsx` は lp-1 と共有される。lp-2 用フォント・トークンを **layout に足すと lp-1 に影響する**ため、
`lp2-tokens.css` は `lp-2/page.tsx` 側で import し、フォントは `fonts.ts` に追加した変数を
lp-2 のルート要素にのみ付与する。lp-1 の描画は変更しない。

---

## 5. デザイントークン

PSD の全テキストレイヤーを集計して抽出した実測値。同系統の微差は統合する。

### 5.1 カラー

| トークン | 値 | PSD 上の実際の値(統合対象) | 用途 |
|---------|-----|------------------------|------|
| `--lp2-ink` | `#131A3E` | `#141C3D` `#06133D` `#000914` `#0C1233` | 見出し・本文の濃紺 |
| `--lp2-primary` | `#5757EF` | `#5856EF` `#5562F1` `#3858FF` | 紫。FAQ・強調枠 |
| `--lp2-accent` | `#FD1C5C` | `#F8026F` `#F8006F` `#F7006B` | ピンク。実績・強調 |
| `--lp2-highlight` | `#FAF26E` | `#FDF263` `#FEF579` `#FFF66D` `#FDF251` `#FCF251` | 黄。見出しハイライト |
| `--lp2-blue` | `#284FA9` | — | セクション見出しの青 |
| `--lp2-cyan` | `#00D0C0` / `#1DE5FF` | — | FV ラベル・装飾 |
| `--lp2-orange` | `#FEB325` | — | VOICE 番号 |
| `--lp2-white` | `#FFFFFF` | — | 反転文字 |
| `--lp2-grey` | `#333333` | — | 本文グレー |

> 微差の統合により、色数は 30 種 → 9 トークンに削減される。統合の可否は実装時に
> レンダリング結果を PSD と重ねて目視確認する。

### 5.2 字送り(letter-spacing)

PSD の tracking 値は以下に集中している。`tracking / 1000` em で CSS に変換する。

| CSS 値 | PSD tracking | 出現数 |
|--------|-------------|-------|
| `0.1em` | 100 | 173 |
| `0.06em` | 60 | 133 |
| `0.08em` | 80 | 59 |
| `0.04em` | 40 | 31 |
| `0.14em` | 140 | 22 |
| `0.5em` | 500 | 18 |
| `0.12em` | 120 | 16 |

`0.5em` は英字セクションラベル(`RESULTS` 等)専用。

### 5.3 フォントサイズ

PC の実測値は 9px 〜 174px に分布。頻出帯は 16px(68) / 15px(63) / 22px(44) / 18px(38) / 12px(32) / 9px(32)。
**PSD の変形行列を適用した補正済みの値**を使用する(Photoshop はレイヤー変形前のポイント数を保持するため、
生の値をそのまま使うと実サイズを誤る)。

---

## 6. フォント方針

### 6.1 使用不可フォントと代替

デザインには商用フォント(Adobe Fonts 収録書体)が使われているが、**Adobe Creative Cloud を契約していないため使用できない**。
また §2.1 の理由で画像化による再現も不可能。したがって全て無料 Web フォントに置換する。

| 用途 | 原本 | 出現 | 採用フォント | ライセンス |
|------|------|-----|------------|-----------|
| 見出し・CTA・強調 | 筑紫ゴシック H / E | 114 | **Zen Kaku Gothic Antique** 900 / 700 | OFL |
| 本文・カード・FAQ・比較表 | Noto Sans CJK JP | 235 | **Noto Sans JP** 400 / 500 / 700 | OFL |
| 英字セクションラベル | FuturaPT Heavy Oblique | 36 | **Jost** 700 + `font-style: oblique` | OFL |
| STEP ラベル | DIN 2014 | 10 | **Barlow Semi Condensed**(既存 `fonts.ts` に導入済み) | OFL |
| STEP 番号・VOICE 番号 | Avenir / Futura | 22 | **Jost** | OFL |
| 給付金バッジ | 筑紫明朝 | 8 | **Shippori Mincho B1** | OFL |
| STEP 装飾の「0」 | Confiteria Script | 16 | 装飾 1 文字のみ。CSS の擬似要素で描く、もしくは SVG 化 | — |

**選定根拠**: 筑紫ゴシックはオールドスタイルの温かみを意図した人文的書体であり、
同じ設計思想を持つ Zen Kaku Gothic Antique が最も近い。比較検証では M PLUS 1p Black が
太さの面で近かったが、幾何学的な骨格のため字の性格が合わない。

### 6.2 実装上の注意

- 代替フォントは原本と**字面の大きさが異なる**ため、フォントサイズと字送りの微調整が必要。
  PSD レンダリングと実装のスクリーンショットを重ねて詰める。
- Zen Kaku Gothic Antique Black は原本(Heavy)よりわずかに軽い。
- Google Fonts は日本語を `unicode-range` で分割配信するため、見出し用に別ファミリーを
  追加しても実際に読み込まれるのは使用文字のチャンクのみ。パフォーマンス上の問題はない。
- Next.js Font Optimization(`next/font/google`)で読み込み、`fonts.ts` に集約する。

---

## 7. 動的挙動

スクロール連動アニメーションは**なし**(確認済み)。実装が必要な挙動は以下のみ。

| セクション | 挙動 | 実装方針 |
|-----------|------|---------|
| ヘッダー | 固定ナビ。アンカーリンク(強み/実績/料金/特典/FAQ) | `position: fixed` + `scroll-margin-top` |
| SP ドロワー | ハンバーガー開閉(sp12_menu) | `useState` + `aria-expanded`。開いている間は body スクロールロック |
| `sec_result` | 3D 風カルーセル(中央 大 / 中 / 小 の 5 枚 + 左右矢印 + ドット) | Swiper の `coverflow` 系。既存リポジトリに Swiper 12 導入済み |
| `sec_skill` | **カリキュラムのアコーディオン(SP のみ)** | §7.1 参照。PC は 5 枚とも常時展開 |
| `sec_voice` | 「続きを読む」 | アコーディオン展開(モーダルではない。カード内で伸長) |
| FAQ | アコーディオン 8 問。初期状態は Q1 のみ開 | `<details>/<summary>` ベース。JS 不要で実装可 |
| `sec_graph` / `sec_difference` | 比較表 | SP では横スクロール(`overflow-x: auto`)。表組みは `<table>` を使う |
| `sec_counselling` | Jicoo 埋め込み | `Lp1Cta.tsx` の実装を流用(IntersectionObserver で遅延ロード) |

### 7.1 SKILLS カリキュラムのアコーディオン(SP)

`seitosama_lp_sp04.psd` に**非表示アートボード `sp4_open.jpg`(750×4819)** が同梱されており、
これが展開状態のデザインである(表示中の sp04 は 750×2202 = 閉じた状態)。

- 各カード(01_生成AI / 02_AIエンジニアリング / 03_フロントエンド / 04_バックエンド / 05_データ分析)に
  開閉用の矢印 `Arrow012` が配置されている。
- カード内は「技術」(タグ一覧)と「できるようになること」(箇条書き)の 2 ブロック。
- **閉じた状態ではカード見出しのみ**を表示し、タップで両ブロックを展開する。
- **PC(pc4)は 5 枚とも常時展開**でアコーディオンにしない。
  → SP でのみアコーディオン化する(`useIsPc` 相当の判定、もしくは CSS のみで閉じ/開きを切り替える)。
- 初期状態(どのカードが開いているか)はデザイン上すべて閉じている。複数同時に開ける形とする。

#### 誤植の扱い

説明テキストが「気になる項目をタップすると詳細が**聞きます**。」となっているが、
**「開きます」の誤植**と考えられる。実装時は「開きます」に修正する。

### 7.2 Jicoo 埋め込み

```
Widget: https://www.jicoo.com/event_types/7prAIkBVVBVF/widget
Script: https://www.jicoo.com/widget/event_type.js
```

総高 17,000px 超の LP では、ページ最下部のウィジェットを初期ロードする意味がない。
既存 `Lp1Cta.tsx` と同じく `IntersectionObserver`(`rootMargin: 600px`)で
一度だけスクリプトを読み込む方式を踏襲する。

---

## 8. 画像アセット

### 8.1 書き出し方針

入稿済みの `images/` `webp_x2/` はカルーセル画像のみで、かつ `errors.txt` の通り
書き出しに失敗した分がある。**PSD から再書き出しする。**

```bash
python3 scripts/lp2/psd_tool.py manifest scripts/lp2/assets.manifest.json
```

- 出力先: `public/images/lp-2/`、形式は **WebP**
- **テキストは画像に焼き込まない。** 代替フォントを使う以上(§6)、PSD の文字を
  画像として貼ることはできない。`exclude_kinds: ["type"]` でテキストレイヤーを除いて
  書き出し、文字は実テキストで重ねる
- **スクリーン合成のグローは画像化できない。** 単独で書き出すと黒く潰れるため、
  `exclude` で除いて CSS の影やグラデーションで表現する
- **クリッピングマスク前提のレイヤーは単独で書き出さない。** 100% 透明になる。
  クリップ元を含む親グループごと書き出すこと
- 単純図形(矢印・チェック・区切り線)は **SVG か CSS で実装**する。画像にしない
- ロゴは既存の `public/images/lp-1/siid-logo.svg` / `siid-logo-w.svg` を流用する
  (新デザインでも字形は同じ。PSD 側は等倍ラスターのため Retina で不足する)
- `next/image` を使う(`<img>` は ESLint エラー)

### 8.2 アセットはフェーズごとに追加する

**全アセットを先に洗い出すことはしない。** 何を画像にして何を CSS / SVG で組むかは
セクションを実装しながらでないと決まらないため。

実装フェーズごとに `scripts/lp2/assets.manifest.json` へ追記し、マニフェストを
再実行する。マニフェストが「どの PSD のどのレイヤーから何を書き出したか」の記録に
なるので、入稿データが差し替わっても再現できる。

フェーズ 0 時点では FV と ABOUT の 3 点のみ登録済み。

## 9. 動画(FV)

| ファイル | サイズ | 用途 |
|---------|-------|------|
| `/Users/horiguchimasato/Downloads/video/fv-pc.mov` | 5.0MB | PC 版 FV 背景 |
| `/Users/horiguchimasato/Downloads/video/fv-sp.mov` | 1.6MB | SP 版 FV 背景 |

同ディレクトリの `fv.mp4` `fv-mobile-v1.mp4` 等は**使用しない**(確認済み)。

### 9.1 変換・実装方針

- `.mov` のままでは Web 配信に適さないため **H.264 MP4** に変換する。

```bash
./scripts/lp2/convert-video.sh ~/Downloads/video
```

  変換結果: `fv-pc` 5,291,609B → **1,394,973B**、`fv-sp` 1,628,375B → **938,555B**。
- **WebM(VP9)は生成しない。** 実測で常に MP4 より大きく(PC 2.25MB vs 1.92MB、
  SP 1.53MB vs 1.46MB)、容量が倍になるだけで得るものが無かった。
- `<video autoplay muted loop playsinline>` で背景再生。`muted` と `playsinline` は
  iOS Safari の自動再生に必須。
- `poster` に FV の静止画(WebP)を指定し、動画ロード前の白画面を防ぐ。
- PC / SP で別ファイルのため、`<source media="...">` もしくは JS による出し分け。
  **LCP に直結するため、SP で PC 用を読ませない**こと。
- **PSD の FV 背景は動画の仮置き**(Zoom 画面のスクリーンショット + 顔隠しの絵文字)であり、
  実際の動画とは別カット。実装してはいけない。詳細は
  [`lp2-sections/02-fv.notes.md`](./lp2-sections/02-fv.notes.md)。
- **カンプと実際の FV の見え方が異なることはヒアリングで承認済み**(動画を使う)。
  そのため §13.1 / §13.2 の一致率判定では、**FV のみ動画領域を除いて判定する**。
- `preload="metadata"`。FV は初期表示領域のため遅延ロードはしない。

---

## 10. フッター(新規設計)

デザインカンプにフッターが存在しない(PC 全 9 ファイルの高さ合計 17,404px = 結合ファイルの
総高 17,405px と一致し、フッター領域がない)。

しかし以下の理由で**必須**と判断し、新規に設計して追加する。

- 運営会社・プライバシーポリシー・特定商取引法に基づく表記へのリンクがないページは、
  公開ページとして不適切
- **Google 広告の審査でも問題になり得る**(広告 LP には事業者情報の明示が求められる)

### 10.1 内容

既存 `Lp1Footer.tsx` の項目を踏襲し、新デザインのトーン(濃紺 `--lp2-ink` 基調)に合わせる。

| 項目 | リンク先 |
|------|---------|
| 運営会社 | https://bug-fix.org/ |
| プライバシーポリシー | https://bug-fix.org/privacy-policy |
| 特定商取引法に基づく表記 | https://bug-fix.org/law |
| コピーライト | `© 2026 BugFix LLC. All rights reserved.` |

LP は 1 ページ完結のため、サイト内リンク(コース一覧等)は**置かない**。
外部の法的表記リンクとロゴ・コピーライトのみの最小構成とする。

---

## 11. SEO・メタデータ

- `buildPageMetadata()` を使う(CLAUDE.md のルール)。
  旧 LP `lp-1` は独自 metadata を書いているが、あれは旧 LP の head 忠実移植という例外措置。
- `constants/meta.ts` に `pages.lp2` を追加する。
- canonical: `${SITE_URL}/lp-2`
- OGP 画像: FV から書き出した WebP/PNG を `public/images/lp-2/ogp.png` に配置。

### 11.1 noindex の扱い(未確定)

現行 `lp-1` は「新 TOP ページと訴求が重複する」ため noindex(Issue #40)。
新 LP は情報量が TOP ページを上回るため、**index させる価値がある**と考えられる。

一方で `(Main)` の TOP ページとのカニバリゼーションの懸念は残る。

→ **実装は `noindex` を切り替え可能な形にしておき**(`buildPageMetadata(pages.lp2, { noindex: true })`
の 1 行差)、公開直前に判断する。判断には TOP ページとのキーワード整理が必要。

---

## 12. アクセシビリティ・品質基準

- 見出しは `<h1>`〜`<h3>` を正しく階層化する(`h1` は FV のメインコピー 1 箇所のみ)。
- 比較表は `<table>` + `<th scope>` を使う(divの格子にしない)。
- FAQ は `<details>/<summary>`、ドロワーは `aria-expanded` / `aria-controls`。
- 画像には意味のある `alt` を付ける。装飾画像は `alt=""`。
- CTA ボタンは `<a>`(アンカー遷移)で実装し、`<div onClick>` にしない。
- コントラスト比: 濃紺背景 + 黄文字など、原本の配色を検証し WCAG AA を下回る箇所は
  トークン値を微調整する。

---

## 13. 完了条件(Definition of Done)

各実装フェーズは、以下 4 条件をすべて満たしてはじめて完了とする。

### 13.1 PC デザイン一致率(横幅 1440px)

ブラウザで**横幅 1440px** のプレビューを表示し、対応する PSD の基準画像と比較する。

```bash
# 基準画像を書き出す(初回のみ)
python3 scripts/lp2/psd_tool.py render ~/Downloads/260902_seitosama/pc --out-dir tmp/psd-ref/pc

# 実装のスクリーンショットと比較する
python3 scripts/lp2/compare.py \
    --reference tmp/psd-ref/pc/pc1.png \
    --actual    tmp/shots/pc1.png \
    --out       tmp/diff/pc1.png
```

`scripts/lp2/compare.py` は **2 つの指標を独立に算出**し、両方の合格を要求する。
単一の指標では「わずかな位置ずれの許容」と「色の誤差の検出」を両立できないため
(位置ずれは輪郭に局所的な大きい差、色ずれは全体に小さい差を生む)。

| 指標 | 算出方法 | 合格ライン | 検出できる誤り |
|------|---------|-----------|--------------|
| **構造一致率** | 4px ぼかし後、最大チャンネル差 24 以内の画素の割合 | **85% 以上** | レイアウト・要素の有無・サイズ |
| **色一致率** | 24px ぼかしで高周波を落とし幅 60px に縮小、許容差 8 で比較。**基準画像側で平坦なセル(局所 σ ≤ 20)に限定** | **85% 以上** | 配色・階調 |

> 色一致率を平坦セルに限定しているのは、文字が乗るセルを含めると Photoshop と
> ブラウザで文字のラスタライズが異なるだけで平均色がずれ、配色が正しくても
> 数値が落ちてしまうため。文字の再現度は構造一致率で判定しており、
> 色軸で二重に減点する必要はない。

較正結果(`pc1.psd` を基準にした実測):

| 比較対象 | 構造一致率 | 色一致率 | 判定 |
|---------|-----------|---------|------|
| 完全一致 | 100.00% | 100.00% | 合格 |
| 全体を 3px ずらす | 88.32% | 96.07% | 合格(軽微なずれは許容される) |
| 明度を 6% 上げる | 100.00% | 73.22% | **不合格**(色の誤りを検出) |
| 幅 1200px で撮影 | 99.70% | 96.19% | 合格(撮影幅に依存しない) |
| 別セクション(pc3) | 14.20% | 10.58% | **不合格** |

#### 構造一致率の実際の上限

文字が面積の大きな割合を占めるセクションでは、配置・書体・サイズが正しくても
**82〜85% 程度で頭打ちになる**。Photoshop とブラウザで文字のラスタライズ
(アンチエイリアスの掛かり方)が異なるためで、実装の誤りではない。

ABOUT セクションで検証した結果:
- 最適なずらし量は dx=1 / dy=1(= 位置はほぼ完全に合っている)
- 1:1 で拡大すると書体・太さ・字幅は一致している
- それでも構造一致率は 82% 前後

このため、判定が 82% 前後で止まるセクションについては、差分画像で
「ずれているのが文字の輪郭だけか」を目視で確認したうえで合否を判断する。

- 高さが一致しない場合、短い方をマゼンタで埋めて全体を比較する。**高さのずれは減点対象**。
- 差分ヒートマップ(不一致画素を赤く重ねた画像)を必ず出力し、レビュー時に提示する。
- 終了コードは合格 0 / 不合格 1。レビューエージェントはこれで機械的に判定する。

### 13.2 SP デザイン一致率(横幅 375px)

ブラウザで**横幅 375px** のプレビューを表示し、対応する SP の PSD(750px = @2x)の
基準画像と比較する。指標・合格ライン・手順は §13.1 と同じ
(`compare.py` は基準画像の幅にスクリーンショットを合わせるため、@2x のままで比較できる)。

### 13.3 リリース前チェックリストの充足

`~/Downloads/チートシート.xlsx` の「リリース前チェックリスト」シート(全 47 項目 / 11 カテゴリ)のうち、
**ローカルサーバーで確認できる項目をすべて満たしていること**。

判定区分:
- **○** = ローカルで確認・充足まで可能。**必須**
- **△** = ローカルで部分的に確認可能。可能な範囲で対応し、残りを未確認項目として記録する
- **×** = 本番公開後・外部サービスが必要。**対象外**(理由を明記してリストアップする)

| 区分 | カテゴリ | 項目 | 確認内容 |
|:--:|---------|------|---------|
| ○ | メタ/SEO | meta title | 「ページ内容 \| サイト名」全角 28 字前後・重要語を前寄せ |
| ○ | メタ/SEO | meta description | ページ要約＋CTA 140〜300 字 |
| ○ | メタ/SEO | canonical | `<link rel="canonical">` に正規 URL |
| ○ | メタ/SEO | robots.txt | `User-agent: *` / `Allow: /` / Sitemap 行 |
| ○ | メタ/SEO | sitemap.xml | 全公開ページの URL を列挙(自動生成) |
| ○ | メタ/SEO | noindex 確認 | `<meta name="robots">` の値。§11.1 の判断に従う |
| ○ | メタ/SEO | html lang | `<html lang="ja">` |
| ○ | メタ/SEO | 構造化データ | JSON-LD で Organization・BreadcrumbList |
| ○ | OGP/SNS | og:title / og:description | title と同等 or シェア用文言 |
| ○ | OGP/SNS | og:image | 1200×630px の絶対 URL |
| ○ | OGP/SNS | og:url | canonical と同じ絶対 URL |
| ○ | OGP/SNS | og:type | `website` |
| ○ | OGP/SNS | twitter:card | `summary_large_image` |
| × | OGP/SNS | シェア表示確認 | X / Facebook デバッガー。**公開 URL が必要** |
| ○ | favicon | favicon | 32×32 ＋ 16×16 |
| ○ | favicon | apple-touch-icon | 180×180 の png |
| ○ | favicon | theme-color | `<meta name="theme-color">` にメインカラー |
| ○ | favicon | site.webmanifest | name / short_name / icons / theme_color / background_color |
| ○ | パフォーマンス | 画像形式 | WebP or AVIF・品質 80 前後 |
| ○ | パフォーマンス | 遅延読込 | FV 外に `loading="lazy"` |
| ○ | パフォーマンス | CLS 対策 | img に width / height を明示 |
| △ | パフォーマンス | Core Web Vitals | LCP 2.5s / CLS 0.1 / INP 200ms 以下。`npm run build` + `npm start` の本番ビルドで計測。**実回線・CDN の影響は本番でしか測れない** |
| △ | パフォーマンス | Lighthouse | モバイルで各項目 90 以上。同上 |
| ○ | 表示/互換性 | viewport | `<meta name="viewport" content="width=device-width, initial-scale=1">` |
| ○ | 表示/互換性 | レスポンシブ確認 | 375px / 1440px で崩れなし。**§13.1・§13.2 と同じ確認で兼ねる** |
| ○ | アクセシビリティ | alt | 画像内容を説明。装飾画像は `alt=""` |
| ○ | アクセシビリティ | コントラスト比 | 375px / 1440px で元画像の比率が維持されているか |
| ○ | アクセシビリティ | 見出し階層 | h1 は 1 ページ 1 つ・階層を飛ばさない |
| ○ | アクセシビリティ | 構造化データ | schema.org に準じたマークアップ |
| ○ | コーディング | CSS / JS の外部ファイル化 | インライン記述は NG。`link` / `script` で読み込む |
| ○ | コーディング | セマンティック HTML | div / span の乱用を避け、h1〜6・section・article・main・aside・img・a・strong・ul・ol・li・figure・figcaption を適切に使う |
| ○ | 計測/解析 | GA4 | **`(Lp)/layout.tsx` の `Analytics` / `GtmNoScript` を lp-1 と共有するため自動的に入る。** 出力を確認する |
| × | 計測/解析 | Search Console | 所有権確認 ＋ sitemap 送信。**公開後** |
| ○ | 計測/解析 | タグ確認 | GTM / GA4 がテスト用 ID のままでないか |
| △ | 計測/解析 | コンバージョン | 予約完了は Jicoo 側で発生する。ローカルでは CTA クリックのイベント発火まで確認 |
| × | セキュリティ | SSL | 本番証明書。**Vercel / Cloudflare 側** |
| × | セキュリティ | リダイレクト | HTTP→HTTPS・www 有無の 301。**Cloudflare Worker 側**(§`06_migration.md`) |
| △ | フォーム | 送信先 | Jicoo 埋め込みのため受信設定は Jicoo 管理画面側。埋め込み ID が正しいことを確認 |
| △ | フォーム | 到達確認 | ローカルからでもウィジェットが表示・操作できるところまで確認 |
| ○ | コンテンツ/QA | リンク切れ | 内部・外部リンクを一括チェック |
| ○ | コンテンツ/QA | ダミー残り | Lorem ipsum・仮テキスト・仮画像なし |
| ○ | コンテンツ/QA | コピーライト | `© 2026 BugFix LLC.`(年号が最新か) |
| △ | コンテンツ/QA | 404 ページ | LP は 1 ページ構成。**`(Lp)` 配下に not-found が無い**ため、`(Main)` 側の 404 に落ちるかを確認する(§13.3.1) |
| ○ | 法務/インフラ | プライバシーポリシー | フッターからリンク(§10) |
| ○ | 法務/インフラ | 特商法 | フッターからリンク(§10) |
| ○ | 法務/インフラ | Cookie 同意 | **実装する**(決定済み)。§13.4 |
| ○ | 法務/インフラ | 環境変数 | API キー等を `.env` にコミットしない |

#### 13.3.1 404 ページの扱い

`(Lp)` ルートグループには `not-found.tsx` が存在せず、404 は `(Main)` 側
(`src/app/(Main)/[...notFound]`)にフォールバックする。LP から共通クロームを持つ
`(Main)` の 404 に飛ぶ挙動が妥当か、レビュー時に確認する。
問題があれば `(Lp)/not-found.tsx` の追加を別途検討する(本フェーズのスコープ外)。

### 13.4 Cookie 同意バナー

GA4 / GTM を設置するため、Cookie 同意バナーを実装する(決定済み)。

- リポジトリに既存実装は無い。**lp-2 用に新規作成する。**
- 同意前は GA4 / GTM を発火させない(Consent Mode もしくは同意後にタグを注入する方式)。
- 同意状態は `localStorage` に保持し、再訪時にバナーを出さない。
- フッターのプライバシーポリシーリンクと併置する。
- `(Lp)/layout.tsx` は lp-1 と共有のため、**バナーは lp-2 のページ側に置き lp-1 に影響させない**。

### 13.5 実装エージェントとレビューエージェントの分離

品質判定を実装者自身に行わせない。以下の体制とする。

```
実装エージェント          レビューエージェント(サブエージェント)
      │
      ├─ 実装
      ├─ §13.1〜13.3 を自己確認
      └─ 完了報告 ─────────▶ 独立してレビュー
                              ├─ §13.1 PC 一致率を検証
                              ├─ §13.2 SP 一致率を検証
                              ├─ §13.3 チェックリストを検証
                              ├─ 不合格項目があれば修正を実施
                              └─ 全項目合格 ─────▶ フェーズ完了
```

- **レビューエージェントは実装エージェントとは別に起動する。** 実装の文脈を引き継がせない
  (自分の書いたコードを自分で評価すると甘くなるため)。
- レビューエージェントは**指摘だけでなく修正まで行う**。
- **レビューエージェントが全項目で合格を出した時点をもってフェーズ完了**とする。
- 不合格が残る場合は完了報告をせず、修正 → 再レビューを繰り返す。

---

## 14. 実装フェーズ

各フェーズを 1 Issue = 1 PR とし、`docs/spec/04_workflow.md` のフローに従う。

| # | 内容 | 成果物 |
|---|------|-------|
| 0 | **足場整備(完了)** | PSD 解析ツール・差分判定ツール・動画変換・セクション別実測データ(Issue #63) |
| 1 | 基盤整備 | `lp-2/page.tsx` の骨格、`lp2-tokens.css`、`fonts.ts` 追記、`pages.lp2` 追加、書き出しスクリプト |
| 2 | 共通パーツ | `Header` / `DrawerMenu` / `CtaButton` / `SectionLabel` / `Footer` |
| 3 | FV + ABOUT + RESULTS | 動画変換・カルーセル |
| 4 | INSTRUCTOR + STRENGTH + DIFFERENCE | 比較表を含む |
| 5 | 5STEP + SKILLS + RESKILLING SUPPORT | |
| 6 | PRICING + コース比較表 | |
| 7 | VOICE + FREE GIFTS | 「続きを読む」 |
| 8 | FAQ + FREE COUNSELING | Jicoo 埋め込み |
| 9 | 仕上げ | PSD との差分検証、レスポンシブ調整、Lighthouse、メタ・OGP |

各フェーズ完了時に `npm run lint && npm run typecheck` を実行したうえで、
**§13 の完了条件 4 項目をすべて満たすこと**。

---

## 15. 未確定事項

| # | 項目 | 現状の扱い |
|---|------|----------|
| 1 | `noindex` の要否(§11.1) | index 前提で実装。公開直前に判断 |
| 2 | SKILLS 説明文の誤植「詳細が聞きます」(§7.1) | 「開きます」に修正して実装 |
| 3 | 比較表内の競合他社名 | PSD では「競合」「転職特化」と匿名。そのまま実装する |
| 4 | Confiteria Script(装飾の「0」)の代替(§6.1) | 実装時に SVG 化 or 擬似要素で対応 |
| 5 | VOICE「続きを読む」の実原稿 | **仮テキストで実装する。公開前に実原稿へ差し替えが必要** |
| 6 | 実績数値(88% / 92% / 13万人)の更新頻度 | 静的実装。更新は都度コード修正 |

> 上記は、デザイナーに確認できない状態のため現時点のデータから妥当と判断した前提。
> 誤りが判明した場合の修正コストはいずれも軽微(文言差し替え程度)。
